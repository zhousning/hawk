- provide(:title, t('.title'))

.container
  .row
    .col-md-3
      %ul.list-group.list-group-flush.pt-3
        - @labs.each do |lab|
          = link_to lab.title, lab_path(lab), :class => "list-group-item"
    .col-md-9
      = @lab.content.html_safe
