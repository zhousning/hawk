- provide(:title, t('.title'))

- content_for :main_body do
  .container-fluid
    .row
      .col-md-3
        = render "layouts/system_search"
    .row.buyer-table-ctn
      .col-md-12
        %table.table.text-center.table-bordered.table-list-search
          %thead
            %tr
              %th #
            
              %th 
                = Setting.manages.title
              %th 
                = Setting.manages.content
              %th
          %tbody
            - @manages.each_with_index do |manage, index|
              %tr
                %td
                  = index+1
              
                %td
                  = manage.title
                %td
                  = manage.content
                %td
                  = link_to "查看", manage_path(manage), :class => "btn btn-primary btn-sm"

= render "layouts/control_panel"
